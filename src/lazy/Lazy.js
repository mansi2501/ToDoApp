import React from 'react';

const Lazy = () => {
    return (
        <div>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
            <p>This is check for lazy loading</p>
        </div>
    );
};

export default Lazy;